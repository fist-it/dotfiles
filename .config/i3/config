# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $modopt Mod1
set $modcom Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
# exec --no-startup-id nm-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Colorscheme
set $bgcolor    #523d64
set $in-bgcolor #363636
set $text       #ffffff
set $u-bgcolor  #ff0000
set $indicator  #a8a3c1
set $in-text    #969696

#                       border          background      text            indicator (a line which shows where the next window will be placed)     
client.focused          $bgcolor        $bgcolor        $text           $indicator
client.unfocused        $in-bgcolor     $in-bgcolor     $in-text        $in-bgcolor
client.focused_inactive $in-bgcolor     $in-bgcolor     $in-text        $in-bgcolor
client.urgent           $u-bgcolor      $u-bgcolor      $text           $u-bgcolor 

default_border pixel 1

# Gaps
gaps inner 8
gaps outer 14

# Use Mouse+$modopt to drag floating windows to their wanted position
floating_modifier $modopt

# move tiling windows via drag & drop by left-clicking into the title bar,
# or left-clicking anywhere into the window while holding the floating modifier.
tiling_drag modifier titlebar

# start a terminal
bindsym $modopt+t exec kitty
bindsym $modopt+f exec firefox-developer-edition
bindsym $modopt+p exec pavucontrol

# kill focused window
bindsym $modcom+q kill

# start rofi (a program launcher)
bindsym $modopt+space exec rofi -show drun
bindsym $modopt+s exec rofi -show ssh

# Screenshots tool -- save to ~/screenshots AND copy to clipboard
bindsym $modcom+Shift+s exec flameshot gui -c -p ~/screenshots
bindsym $modcom+Shift+f exec flameshot full -c -p ~/screenshots


# change focus
bindsym $modopt+h focus left
bindsym $modopt+j focus down
bindsym $modopt+k focus up
bindsym $modopt+l focus right

# move focused window
bindsym $modopt+Shift+h move left
bindsym $modopt+Shift+j move down
bindsym $modopt+Shift+k move up
bindsym $modopt+Shift+l move right

# split in horizontal orientation
bindsym $modopt+g split h

# split in vertical orientation
bindsym $modopt+v split v

# enter fullscreen mode for the focused container
bindsym $modopt+Shift+t fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $modopt+Shift+s layout stacking
# bindsym $modopt+Shift+w layout tabbed
bindsym $modopt+Shift+e layout toggle split

# toggle tiling / floating
bindsym $modopt+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $modopt+d focus mode_toggle

# focus the parent container
bindsym $modopt+a focus parent

# focus the child container
#bindsym $modopt+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1: Main"
set $ws2 "2: Side"
set $ws3 "3: Spotify"
set $ws4 "4: Chat"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $modopt+1 workspace number $ws1
bindsym $modopt+2 workspace number $ws2
bindsym $modopt+3 workspace number $ws3
bindsym $modopt+4 workspace number $ws4
bindsym $modopt+5 workspace number $ws5
bindsym $modopt+6 workspace number $ws6
bindsym $modopt+7 workspace number $ws7
bindsym $modopt+8 workspace number $ws8
bindsym $modopt+9 workspace number $ws9
bindsym $modopt+0 workspace number $ws10

# move focused container to workspace
bindsym $modopt+Shift+1 move container to workspace number $ws1
bindsym $modopt+Shift+2 move container to workspace number $ws2
bindsym $modopt+Shift+3 move container to workspace number $ws3
bindsym $modopt+Shift+4 move container to workspace number $ws4
bindsym $modopt+Shift+5 move container to workspace number $ws5
bindsym $modopt+Shift+6 move container to workspace number $ws6
bindsym $modopt+Shift+7 move container to workspace number $ws7
bindsym $modopt+Shift+8 move container to workspace number $ws8
bindsym $modopt+Shift+9 move container to workspace number $ws9
bindsym $modopt+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $modopt+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $modopt+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $modopt+Shift+Control+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

#lock screen
bindsym $modcom+Ctrl+l exec --no-startup-id ~/.config/config_scripts/lock.sh

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize shrink height 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $modopt+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $modopt+r mode "default"
}

bindsym $modopt+r mode "resize"

bindsym $modopt+Shift+w move workspace to output next

exec_always --no-startup-id polybar &
exec_always feh --bg-fill ~/.local/wallpapers/tapeta_gory.jpg &
exec_always --no-startup-id picom &

for_window [class="pavucontrol"] floating enable

exec_always --no-startup-id xset s on &
exec_always --no-startup-id xset s 300 &
exec_always --no-startup-id xautolock -time 4 -corners +-+- -cornersize 30 -locker ~/.config/config_scripts/lock.sh &
exec_always --no-startup-id ~/.config/config_scripts/double-xrandr.sh &
exec_always --no-startup-id openrgb -p basic &
exec_always --no-startup-id nvfancontrol &
